{
  "name": "dino-clone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:assets": "cpx \"assets/**/*\" \"public/assets\"",
    "dev": "npm run sync:assets && vite",
    "build": "npm run sync:assets && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.8"
  },
  "prettier": {
    "singleQuote": true
  }
}
